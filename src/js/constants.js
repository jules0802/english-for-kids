const button = document.querySelector('.game-btn');
const categoryPage = document.querySelector('.category');
const toggler = document.querySelector('#switch');
const mainPage = document.querySelector('.main-page');
const switcher = document.querySelector('.switcher-container');
const menu = document.querySelector('.menu');
//const MODES = { train: 'train', play: 'play' };
const hamburger = document.querySelector('.hamburger');
const cardObjectList = [];



export { button, categoryPage, toggler, mainPage, switcher, menu, hamburger, cardObjectList}